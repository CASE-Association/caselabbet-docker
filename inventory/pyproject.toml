[project]
name = "CASE-Inventory-Collator"
version = "0.2.0"
description = "Merges IZettle and extra google sheet into a single .json file for caselabbet.se/inventory"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
    "schedule>=1.2.2",
]
